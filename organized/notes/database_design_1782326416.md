# project framework thoughts
- layout view needs to be built with dynamic react components
- backend routes should use fastapi endpoints to keep parsing speed fast
- storage layer: sqlite works fine for the pilot model but we will need structural table schemas
- security rules: use basic token payload mapping checks to protect client records from leaking out